# 草莓管家 (Strawberry App) 开发进度记录

## 📅 当前进度 (UI 阶段完成)
- [x] **全局样式**：已同步 `strawberry-chat.html` 的色彩规范（如 `--red`, `--cream` 等）和字体设置。
- [x] **图标系统**：已成功集成 Remix Icon，并替换了所有占位图标（菜单、麦克风、发送、设置等）。
- [x] **侧边抽屉 (`side-drawer.vue`)**：完成了头部渐变、新建对话按钮、历史记录列表及底部设置按钮的 UI 还原。
- [x] **首页 (`home.vue`)**：
  - 完成了顶部导航栏的精简。
  - 实现了带有快捷提问的欢迎卡片。
  - 实现了用户与 AI 的消息气泡样式及“正在输入”动画。
  - 完成了底部胶囊式输入框及独立发送按钮的布局。
  - 添加了全屏语音录制遮罩层。
- [x] **设置页 (`settings.vue`)**：完成了卡片式布局、字体大小选择器、API 配置项及关于区域的 UI 还原。
- [x] **适配性优化**：针对 375x812 (iPhone X) 进行了深度适配，修复了右侧内容溢出、输入框遮挡及安全区域 (Safe Area) 的问题。

## 🚧 待完善功能 (逻辑与交互阶段)
1. **对话逻辑完善**：
   - 确保 `store/chat.js` 能正确管理会话状态。
   - 完善与 DeepSeek API 的流式对接，处理网络异常和加载状态。
   - 确保 GraphRAG 知识库查询能正确拼接到 Prompt 中。
2. **语音功能真实接入**：
   - 替换目前的模拟录音逻辑，接入真实的 `uni.getRecorderManager()`。
   - 对接讯飞 ASR（或其它语音识别服务），将语音转换为文本。
   - （可选）实现 AI 回复的 TTS 语音播报功能。
3. **历史记录管理**：
   - 实现本地存储（Storage）保存历史对话记录。
   - 完善侧边栏历史记录的点击切换和删除逻辑。
4. **设置项生效**：
   - 确保设置页中的“字体大小”能动态响应到全局。
   - 确保 API Key 和知识库地址的修改能即时生效。